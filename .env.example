# SACCO+ Admin Portal - Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# APPLICATION MODE
# ============================================================================
# Set to 'true' to run with mock data (no Supabase connection required)
# Set to 'false' or omit for production mode (requires Supabase)

VITE_USE_MOCK_DATA=false

# ============================================================================
# GEMINI API (Optional - Only for AI/OCR features)
# ============================================================================
# This is used server-side in Supabase Edge Functions, not in the frontend
# Get your key from: https://aistudio.google.com/app/apikey
# Note: This should be configured in Supabase Functions secrets, not here

# GEMINI_API_KEY=your-gemini-api-key-here

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================
# Node.js version (used by Cloudflare Pages and local development)
# See .nvmrc file for version specification

# NODE_VERSION=18

# ============================================================================
# NOTES
# ============================================================================
# - Never commit .env.local to version control
# - All VITE_* variables are exposed to the browser (by design)
# - Never put secrets in VITE_* variables
# - For Cloudflare Pages: Set these in Pages → Settings → Environment Variables
# - For local development: Create .env.local from this template
