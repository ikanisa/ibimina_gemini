/**
 * Shared Module
 * 
 * Contains reusable components, hooks, and utilities used across all features.
 * 
 * @example
 * import { Button, Modal, Card } from '@/shared/components';
 * import { useDebounce, useTheme } from '@/shared/hooks';
 */

export * from './components';
export * from './hooks';
