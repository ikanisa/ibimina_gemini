/**
 * Communications Settings - WhatsApp Messaging
 * @deprecated Use the version in src/features/settings instead
 */

import React from 'react';
import { WhatsAppMessaging } from '@/features/messaging';

export const NotificationsSettings: React.FC = () => {
  return <WhatsAppMessaging />;
};
